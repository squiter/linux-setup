# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# %%hotkey: Run terminal: exec i30sensible-terminal %%
bindsym $mod+Return exec i3-sensible-terminal

# %%hotkey: Kill focused window %%
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
# bindsym $mod+d exec dmenu
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# focus windows with jkl;
# %%hotkey: Focus the left window %%
bindsym $mod+j focus left
# %%hotkey: Focus the window bellow %%
bindsym $mod+k focus down
# %%hotkey: Focus the window above %%
bindsym $mod+l focus up
# %%hotkey: Focus the right window %%
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
# %%hotkey: Focus the left window %%
bindsym $mod+Left focus left
# %%hotkey: Focus the window bellow %%
bindsym $mod+Down focus down
# %%hotkey: Focus the window above %%
bindsym $mod+Up focus up
# %%hotkey: Focus the righ window %%
bindsym $mod+Right focus right

# move focused window
# %%hotkey: Move focused window to left %%
bindsym $mod+Shift+j move left
# %%hotkey: Move focused window down %%
bindsym $mod+Shift+k move down
# %%hotkey: Move focused window up %%
bindsym $mod+Shift+l move up
# %%hotkey: Move focused window to right %%
bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
# %%hotkey: Move focused window to left %%
bindsym $mod+Shift+Left move left
# %%hotkey: Move focused window down %%
bindsym $mod+Shift+Down move down
# %%hotkey: Move focused window up %%
bindsym $mod+Shift+Up move up
# %%hotkey: Move focused window to right %%
bindsym $mod+Shift+Right move right

# split in horizontal orientation
# %%hotkey: Split in horizontal orientation %%
bindsym $mod+h split h

# split in vertical orientation
# %%hotkey: Split in vertical orientation %%
bindsym $mod+v split v

# enter fullscreen mode for the focused container
# %%hotkey: Toggle fullscreen %%
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
# %%hotkey: Change layout to stack %%
bindsym $mod+s layout stacking
# %%hotkey: Change layout to tabs %%
bindsym $mod+w layout tabbed
# %%hotkey: Change layout to split %%
bindsym $mod+e layout toggle split

# toggle tiling / floating
# %%hotkey: Toggle tiling / floating %%
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
# %%hotkey: Change focus between tiling / floating windows %%
bindsym $mod+space focus mode_toggle

# focus the parent container
# %%hotkey: Focus the parent window %%
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# lock i3
# %%hotkey: Lock computer %%
bindsym $mod+Mod1+l exec i3lock-fancy -pg

# switch to workspace
# %%hotkey: Switch to workspace 1 %%
bindsym $mod+1 workspace 1
# %%hotkey: Switch to workspace 2 %%
bindsym $mod+2 workspace 2
# %%hotkey: Switch to workspace 3 %%
bindsym $mod+3 workspace 3
# %%hotkey: Switch to workspace 4 %%
bindsym $mod+4 workspace 4
# %%hotkey: Switch to workspace 5 %%
bindsym $mod+5 workspace 5
# %%hotkey: Switch to workspace 6 %%
bindsym $mod+6 workspace 6
# %%hotkey: Switch to workspace 7 %%
bindsym $mod+7 workspace 7
# %%hotkey: Switch to workspace 8 %%
bindsym $mod+8 workspace 8
# %%hotkey: Switch to workspace 9 %%
bindsym $mod+9 workspace 9
# %%hotkey: Switch to workspace 10 %%
bindsym $mod+0 workspace 10

# move focused container to workspace
# %%hotkey: Move window to workspace 1 %%
bindsym $mod+Shift+1 move container to workspace 1
# %%hotkey: Move window to workspace 2 %%
bindsym $mod+Shift+2 move container to workspace 2
# %%hotkey: Move window to workspace 3 %%
bindsym $mod+Shift+3 move container to workspace 3
# %%hotkey: Move window to workspace 4 %%
bindsym $mod+Shift+4 move container to workspace 4
# %%hotkey: Move window to workspace 5 %%
bindsym $mod+Shift+5 move container to workspace 5
# %%hotkey: Move window to workspace 6 %%
bindsym $mod+Shift+6 move container to workspace 6
# %%hotkey: Move window to workspace 7 %%
bindsym $mod+Shift+7 move container to workspace 7
# %%hotkey: Move window to workspace 8 %%
bindsym $mod+Shift+8 move container to workspace 8
# %%hotkey: Move window to workspace 9 %%
bindsym $mod+Shift+9 move container to workspace 9
# %%hotkey: Move window to workspace 10 %%
bindsym $mod+Shift+0 move container to workspace 10

# %%hotkey: Reload the configuration file %%
bindsym $mod+Shift+c reload
# %%hotkey: Restart i3 (preserves your layout/session, can be used to upgrade i3) %%
bindsym $mod+Shift+r restart
# %%hotkey: Exit i3 - Log out X session %%
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # %%hotkey: In resize mode: Shrink the window's width %%
        bindsym j resize shrink width 10 px or 10 ppt
        # %%hotkey: In resize mode: Grow the window's width %%
        bindsym k resize grow height 10 px or 10 ppt
        # %%hotkey: In resize mode: Shrink the window's height %%
        bindsym l resize shrink height 10 px or 10 ppt
        # %%hotkey: In resize mode: Grow the window's height %%
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        # %%hotkey: In resize mode: Shrink the window's width %%
        bindsym Left resize shrink width 10 px or 10 ppt
        # %%hotkey: In resize mode: Grow the window's widht %%
        bindsym Down resize grow height 10 px or 10 ppt
        # %%hotkey: In resize mode: Shrink the window's height %%
        bindsym Up resize shrink height 10 px or 10 ppt
        # %%hotkey: In resize mode: Grow the window's height %%
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        # %%hotkey: Exit the resize mode %%
        bindsym Return mode "default"
        # %%hotkey: Exit the resize mode %%
        bindsym Escape mode "default"
}

# https://thomashunter.name/i3-configurator/
# class                 border  bground text    indicator child_border
client.focused          #A6B3CC #B362FF #2B2942 #FAD000   #B362FF
client.focused_inactive #494685 #565575 #EEEEEE #565575   #565575
client.unfocused        #494685 #1E1E3F #808080 #292D2E   #1E1E3F
client.urgent           #2F343A #A599E9 #EEEEEE #A599E9   #A599E9
client.placeholder      #1E1E3F #1E1E3F #EEEEEE #1E1E3F   #1E1E3F

client.background       #EEEEEE

# %%hotkey: Enter in resize mode %%
bindsym $mod+Control+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        # pip install py3status
        status_command py3status
        # tray_output primary
        font pango:Inconsolata 13

        colors {
            background #1E1E3F
            statusline #EEEEEE
            separator  #808080

            focused_workspace  #2B2942 #B362FF #100E23
            active_workspace   #2B2942 #494685 #EEEEEE
            inactive_workspace #2B2942 #494685 #808080
            urgent_workspace   #2B2942 #A599E9 #EEEEEE
            binding_mode       #2B2942 #A599E9 #EEEEEE
      }
}

# Customizations
# Polybar
# exec_always --no-startup-id $HOME/.config/polybar/launch.sh

# %%hotkey: Run gsimplecal %%
bindsym $mod+Contrl+c exec gsimplecal
# %%hotkey: Open print-screen dialog %%
bindsym --release Print exec "gnome-screenshot -a -i"
# %%hotkey: Just take a print-screen %%
bindsym --release Shift+Print exec "gnome-screenshot -a"
# %%hotkey: Just take a print-screen of selected window %%
bindsym --release Control+Print exec "gnome-screenshot -w"
# %%hotkey: Just take a print-screen %%
bindsym --release Control+Shift+Print exec "gnome-screenshot"

# %%hotkey: Rofi: drun - Run applications %%
bindsym $mod+d exec rofi -show drun -show-icons
# %%hotkey: Rofi: window - Show openned windows %%
bindsym $mod+Tab exec rofi -show window -show-icons
# %%hotkey: Rofi: add-task - Add a task in org-mode %%
bindsym $mod+o exec rofi-add-task.sh
# %%hotkey: Rofi: last-pass - Copy a password to clipboard %%
bindsym $mod+Control+p exec rofi-last-pass.sh
# %%hotkey: Rofi: Bitwarden - Copy a password to clipboard %%
bindsym $mod+p exec bwmenu
# %%hotkey: Rofi: yubikey - Copy a two-factor code to clipboard %%
bindsym $mod+y exec rofi-yubikey.sh
# %%hotkey: Rofi: keys - Show all current keybinds %%
bindsym $mod+b exec dmenu-i3-hotkeys.py
# %%hotkey: Rofi: calc - Open calculatoR
bindsym $mod+c exec --no-startup-id "rofi -show calc -modi calc -no-show-match -no-sort"
# %%hotkey: Rofi: kill - Kill a program
bindsym $mod+Control+k exec rofi-kill.sh

# %%hotkey: Rofi: Kaomoji
bindsym $mod+Mod1+k exec ~/dev/code/kaomoji-rofi/kaomoji.sh
# %%hotkey: Rofi: Emoji
bindsym $mod+Mod1+e exec --no-startup-id "rofi -show emoji -modi emoji"


# Pulse Audio controls
bindsym XF86AudioRaiseVolume exec --no-startup-id amixer set Master 2+ #increase sound volume
bindsym XF86AudioLowerVolume exec --no-startup-id amixer set Master 2- #decrease sound volume
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute 0 toggle # mute sound

# Media player controls
bindsym XF86AudioPlay exec playerctl play-pause
bindsym XF86AudioPause exec playerctl pause
bindsym XF86AudioStop exec playerctl stop
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous

# %%hotkey: Toggle microphone
bindsym $mod+m exec mic-toggle

# %%hotkey: Send the focused window to scratchpad - like minimize %%
bindsym $mod+Shift+minus move scratchpad
# %%hotkey: Retrieve the first scratchpad window %%
bindsym $mod+minus scratchpad show


# %%hotkey: Move to next workspace %%
bindsym $mod+Control+semicolon workspace next
# %%hotkey: Move to previous workspace %%
bindsym $mod+Control+h workspace prev
# %%hotkey: Move to next workspace %%
bindsym $mod+Control+Right workspace next
# %%hotkey: Move to previous workspace %%
bindsym $mod+Control+Left workspace prev

# Startup
# moved to ~/.xinitrc
# exec compton
# Dropbox start was fixed when I use NixOS
# exec dropbox start
# keyboard configuration was moved to my NixOS config
# exec ~/scripts/set-keyboard
exec flock -w 0 /tmp/rescuetime.lock -c "rescuetime"
# exec flock -w 0 /tmp/sound-switcher.lock -c "indicator-sound-switcher"
exec flock -w 0 /tmp/caffeine.lock -c "caffeine-indicator"
# exec flock -w 0 /tmp/autokey.lock -c "autokey"
# exec nm-applet
# exec monitor-just-hdmi
exec feh --randomize --bg-fill ~/Dropbox/personal/images/wallpapers-ultrawide/*

# Assign applications to your workspaces
assign [class="^Franz"] 10
assign [class="^Spotify"] 9
assign [class="^Fortinet"] 8
assign [class="^Forticlient"] 8

# i3-gaps - https://gist.github.com/boreycutts/6417980039760d9d9dac0dd2148d4783
for_window [class="^.*"] border pixel 3
gaps inner 0
