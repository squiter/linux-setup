#!/bin/sh

TIME="$1"
MSG=$(echo "$2" | sed -e 's/\(:\)[a-zA-Z]*\(:\)/\1\2/' | sed -e 's/://g') # removes orgmode tags
ICON="$3"

notify-send -i "$ICON" "ORGMODE!" "Appointment in $TIME minutes:\n$MSG\n"
#espeak "Hey Skeeter! $MSG"
aplay "~/bin/LTTP_ItemFanfare_Stereo.wav"
